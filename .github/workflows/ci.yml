name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm
      - name: Install Packages
        run: npm ci
      - name: Lint
        run: npm run lint
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm
      - name: Install Packages
        run: npm ci
      - name: Build
        run: npm run compile
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm
      - name: Install Packages
        run: npm ci
      - name: Build
        run: npm run compile
      - name: Run e2e tests
        run: xvfb-run -a npm test
  multiroot-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm
      - name: Install Packages
        run: npm ci
      - name: Build
        run: npm run compile
      - name: Run multi-root e2e tests
        run: xvfb-run -a npm run test:multiroot
